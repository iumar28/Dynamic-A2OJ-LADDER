#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n,m;
    cin>>n>>m;
    vector<string> marks(n);
    for(int i=0;i<n;i++)
    {
        cin>>marks[i];
    }
    for(int i=0;i<m;i++)
    {
        
    }
}